export const metadata = {
  title: "Meta Chef",
  description: "Your Next Level Personal Chef",
};

import "../global.css";

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
